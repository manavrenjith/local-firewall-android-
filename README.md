# local-firewall-android-
Android application that implements a local, on-device network firewall using the Android VpnService API. It intercepts all device network traffic at the IP layer and applies per-application, per-network policy decisions without requiring root access or external VPN infrastructure.
